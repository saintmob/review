{
  "entities": {
    "ShowPlan": {
      "title": "ShowPlan",
      "description": "Editable show plan used by the public page and admin backend",
      "type": "object",
      "properties": {
        "data": {
          "type": "object",
          "description": "MemoState payload containing preheat, stages, staff, checklist, and audience sections"
        },
        "title": {
          "type": "string",
          "description": "Human-readable plan title"
        },
        "schemaVersion": {
          "type": "string",
          "description": "Client data schema version"
        },
        "updatedAt": {
          "type": "timestamp",
          "description": "Server write timestamp"
        },
        "updatedBy": {
          "type": "string",
          "description": "Identifier for the backend writer"
        }
      }
    }
  },
  "firestore": {
    "/showPlans/ensemble-flow": {
      "schema": "ShowPlan",
      "description": "Single source of truth for synchronized show plan content"
    }
  }
}
